# Use hoisted linker so node_modules is flat (npm-compatible).
# This makes the Docker runtime COPY of node_modules reliable —
# pnpm's default isolated linker uses symlinks that break across stages.
node-linker=hoisted
